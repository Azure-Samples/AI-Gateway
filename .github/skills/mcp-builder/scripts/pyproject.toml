[project]
name = "mcp-builder-eval-scripts"
version = "0.1.0"
description = "Helper scripts for MCP server evaluation (skill internal)."
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.39.0",
    "mcp>=1.1.0",
]

[tool.uv]
package = false
